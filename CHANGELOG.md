# 更新日志

本项目所有值得注意的变更记录于此。版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.0.2] - 2026-07-16

### 修复
- 首页恢复范围联动的作业看板摘要，显示区间缺交、红黄预警与全勤人数，并可进入完整作业看板。
- 作业登记页头部增加班级范围菜单，手机和桌面均可在录入前明确切换教学班。
- 教学班菜单只返回教师当前任教学科；遗留他科当前班自动回退到“全部”，显式他科班的作业看板和录入请求返回 409。
- 作业“全部”范围只聚合当前学科合法教学班成员，不再回退所有学科教学班或全花名册。
- 作业标签、教学班提交率、记录管理、学生摘要和花名册统一使用同一合法班集合；按记录/学生/教学班 ID 的越权更新或删除会被拒绝。
- `subject` 为 `NULL`、空串或纯空白的旧班继续作为当前学科兼容班，并在菜单、默认并集、显式范围和提交率中保持一致。
- 花名册新增必须选择具体合法教学班，并原子创建或补全学生与教学班成员关系，不再产生新增后不可见的孤儿记录；旧客户端不传 `teaching_class_id` 会返回 422。
- 作业、智能录入和特殊记录均在空白/格式校验前先校验显式教学班，任何文本内容都不能绕过他科班 409。
- 花名册删除若发现学生仍属于范围外教学班则返回 409，不会静默删除他科成员关系；设置页切班时立即隐藏旧 roster，加载失败保持错误态且禁止写操作。
- 修复快速切班请求竞态，旧班响应不能覆盖当前班；加载失败会清空旧数据且不再显示误导性的零指标。
- 新增前端 UI 契约和后端“数学教师 + 遗留物理班”隔离反例，并纳入 CI；隔离子进程在断言成功后显式退出，避免线程清理延迟造成误报超时，断言异常仍保持非零退出。

[2.0.2]: https://github.com/wangzuoyuan/Performance-Analysis-Teaching/releases/tag/v2.0.2

## [2.0.1] - 2026-07-16

### 部署
- 新增 GitHub Container Registry 自动发布流程，为 backend/frontend 提供 `linux/amd64`、`linux/arm64` 多架构公开镜像。
- `docker-compose.yml` 默认支持按 `IMAGE_TAG` 直接拉取 GHCR 镜像，同时保留 `--build` 本地构建能力。
- 新增 `compose.env.example`，README/DEPLOY 补充升级前备份、直接拉取、健康检查、回滚与离线导入步骤。
- GitHub Actions 升级到当前 Node.js 24 运行时版本，并在 CI 中增加 `docker compose config` 校验。

[2.0.1]: https://github.com/wangzuoyuan/Performance-Analysis-Teaching/releases/tag/v2.0.1

## [2.0.0] - 2026-07-16

### 破坏性变更
- 应用从班主任多学科分析重构为**任课教师唯一学科、多教学班**分析；页面、API、导出、报告和 AI 工具不再提供总分、多学科矩阵或跨学科组合统计。
- `teaching_class_id` 成为教学班硬边界；未指定时仅表示当前学科所有合法教学班成员的去重并集，不再退化为全年级。
- 各教学班使用独立 competition ranking；高二/高三选考科目使用 `grade_score`，其他情况使用 `raw_score`。

### 数据与兼容
- 新上传只持久化教师当前任教学科，parser 不再生成 `total_scores`、`student_totals` 或 `total_averages` 契约。
- `TotalScore` 仅保留旧 SQLite 启动、备份/恢复及整场考试删除兼容，不参与生产业务读写，也不会自动删除历史数据。
- 上传与成员同步合并为单事务，失败后不残留 `Exam`、`Upload` 或 `SubjectScore` 半成品。

### 范围与质量
- Dashboard、教学班对比、WeeklyFocus、作业相关性、学生画像及 Chat 工具统一限制为当前学科和合法教学班成员；合法 `_anon:` 与无成绩成员在作业、档案和空画像中继续保留。
- 新增教学班越权、跨学科诱饵、身份别名、空成绩成员、事务故障注入、旧库兼容和 parser 契约回归测试。
- 更新 README、AGENTS 与 CLAUDE 开发说明；前端生产构建与完整后端回归已验证。

[2.0.0]: https://github.com/wangzuoyuan/Performance-Analysis-Teaching/releases/tag/v2.0.0

## [1.0.4] - 2026-07-08

### 变更
- **作业记录改为按作业种类归类**：任课老师只面向一个学科，录作业时不再要求区分学科，改为校本作业、周末作业、试卷订正、日常作业等作业种类。
- **智能录入支持混合写法**：同一批文本可混写 `张三校本优秀`、`订正缺交：李四、王五`、`校本差：吴六、赵七`，并正确解析缺交/已交评价与多人名单。
- 作业看板、记录管理、连续预警、学生作业卡片和每日 Excel 导出统一改用“作业种类”文案；缺交 × 成绩页面收敛为总缺交 × 排名总览。

[1.0.4]: https://github.com/wangzuoyuan/Performance-Analysis-Teaching/releases/tag/v1.0.4

## [1.0.3] - 2026-07-07

### 修复
- **同名仅姓名学生跨教学班串数据**：仅姓名占位学号此前按姓名全局生成（`_anon:姓名`），若两个教学班各有一名同名（实为不同人）的仅姓名学生，会共用同一占位学号——给一个班录缺交会同时出现在另一个班的看板里，「全部班级」人数也会误去重。现改为按教学班隔离（`_anon:<教学班id>:姓名`），从根上杜绝串班。
  - `anon_sid_for(name, teaching_class_id)` 带上教学班；新增 `is_class_scoped_anon` / `name_from_anon_sid` 兼容新旧格式。
  - 启动迁移 `_rekey_anon_members_class_scoped` 幂等地把历史 `_anon:姓名` 迁成按班隔离格式；仅属单个班的占位成员，其花名册/缺交/特殊/成长档案一并改指到新学号。

### 工程
- 新增 GitHub Actions CI：后端 `pytest`、前端 `tsc --noEmit` + `next build`。

[1.0.3]: https://github.com/wangzuoyuan/Performance-Analysis-Teaching/releases/tag/v1.0.3

## [1.0.2] - 2026-07-07

### 修复
- **仅姓名教学班成员无法录作业缺交**：在「班级配置」里按姓名添加的教学班成员会落成「仅姓名」占位学号（`_anon:姓名`），但作业录入把成员内连接花名册、`members_of` 又刻意排除占位学号，导致这些成员对作业模块完全不可见——录缺交时提示「未匹配到当前教学班学生」，看板显示「0 名有效学生」。
  - 作业录入改以教学班成员为准匹配（左连花名册回落取姓名），占位成员可按姓名匹配；写入缺交前给缺花名册行的学号补建花名册，记录正常进入看板/预警口径。
  - `members_of` / `all_my_member_ids` 新增 `include_anon` 开关，作业范围纳入仅姓名学生（成绩分析仍默认排除，口径不变）。
  - 新建仅姓名成员时同步补建花名册；启动迁移幂等补齐历史占位成员的花名册（升级后重启一次即自动修复现有数据）。
  - 占位成员「后期补真实学号」时，一并迁移其花名册/缺交/特殊/成长档案。

[1.0.2]: https://github.com/wangzuoyuan/Performance-Analysis-Teaching/releases/tag/v1.0.2

## [1.0.0] - 2026-07-03

首个正式发布版本：任课老师的多教学班成绩与教学质量分析 Web 应用。

### 教学班（核心）
- **多教学班模型**：一位老师配置任意多个教学班；高一＝行政班数字，高二/三支持走班名（`物A1`/`史B3`）。
- **班级配置向导**（`/settings/classes`）：建班、成员维护、四态批量导入（学号/姓名/花名册）、按行政班号一键同步、排序删班、设当前班。
- **总览 + 分班两种仪表盘**，顶栏一键切换。
- **跨学年学情继承**：分班导致学号变更时，以「姓名确认 + 学号对照表」人工建链，让跨学年画像/趋势/档案接续。

### 成绩分析
- **Excel 批量导入**：学生成绩明细表 + 班级均分表；自动识别教学班/走班列，写入 `class_label`。
- **高一 / 高二高三双口径**：高一主三门/五门/九门；高二/三主三门/+3/3+3 与选考等级分。
- 考试详情、可自定义关注段位、段位趋势、排名筛选/频次、进退步排行、班级对比、学生画像。

### 作业 / 档案 / AI / 备份
- **作业跟踪看板**：智能文本录入缺交/请假/迟到，每日趋势/各科占比/排行/连续缺交预警、自动导出 Excel、花名册排除开关、学期配置。
- **缺交 × 成绩相关性**：缺交次数与排名散点 + 各科皮尔逊系数排序。
- **成长/谈话档案**、**本周关注**（合并连续缺交预警、本周激增、最近考试临界/薄弱/偏科、谈话跟进待办）、**家长会一页纸**。
- **数据备份/恢复**（`~/.exam-tracker-backups`，不被初始化清空）。
- **AI 对话助手**：Anthropic / OpenAI 兼容，只读工具查询本地数据后回答。

### 修复
- **作业特殊记录端点崩溃**：`POST /api/homework/special-records` 调用已删除的 `_find_student_id`，任何请假/迟到提交都会 `NameError`；改用 `_resolve_student`，并支持在教学班内按姓名消歧。
- **负面评价被反转**：智能录入里「不认真」「不工整」等负面词因内含正面子串被误判为正面，导致质量预警与评价分布反转；改为负面词优先匹配。

### 部署
- 本地 `run.py` 跨平台一键启动（后端 `:8000` / 前端 `:3000`）。
- Docker / 群晖 NAS 部署：`docker-compose.yml` + `Caddyfile` + `DEPLOY.md`。

[1.0.0]: https://github.com/wangzuoyuan/Performance-Analysis-Teaching/releases/tag/v1.0.0
