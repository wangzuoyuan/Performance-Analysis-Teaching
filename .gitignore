.env
node_modules/
.next/
tsconfig.tsbuildinfo
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
*.sqlite
raw/

# Docker 部署的数据卷（含 db.sqlite / 备份 / 导出，绝不入库）
/data/

.DS_Store

# 本地开发工具配置（Claude Code 预览启动器等）
.claude/

# 设计系统同步产物（可再生，不入库）
.ds-sync/
ds-bundle/
frontend/.ds-barrel.tsx
frontend/.ds-compiled.css
frontend/.ds-shim.ts
frontend/.ds-tailwind.config.js

# Playwright MCP 截图输出（本地验证产物，不入库）
.playwright-mcp/
