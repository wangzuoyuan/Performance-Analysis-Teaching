# Docker Compose 会自动读取项目根目录的 .env。
# 固定版本最稳妥；如希望每次拉取最新正式版，可改为 latest。
IMAGE_TAG=2.0.3

# 默认使用公开 GHCR 镜像。自建镜像仓库时可覆盖这两个地址。
BACKEND_IMAGE=ghcr.io/wangzuoyuan/performance-analysis-teaching-backend
FRONTEND_IMAGE=ghcr.io/wangzuoyuan/performance-analysis-teaching-frontend
